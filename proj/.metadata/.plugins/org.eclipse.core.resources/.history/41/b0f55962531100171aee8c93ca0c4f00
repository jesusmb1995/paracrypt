#include "GPUCipherDevice.hpp"

int paracrypt::GPUCipherDeviceinvalid use of ‘this’ in non-member function::getGridSize(int n_blocks, int threadsPerCipherBlock) {
    int gridSize = n_blocks * threadsPerCipherBlock / this->getThreadsPerThreadBlock();
    return gridSize;
}
